apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "masterportal.fullname" . }}-test-connection"
  labels:
    {{- include "masterportal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "masterportal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
