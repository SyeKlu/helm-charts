apiVersion: v1
kind: Service
metadata:
  name: {{ include "masterportal.fullname" . }}
  labels:
    {{- include "masterportal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "masterportal.selectorLabels" . | nindent 4 }}
